import styled from "styled-components";

export const DropdownWrapper = styled.div`
  .szh-menu {
    border-radius: 0 0 8px 8px;
    padding-bottom: 0px;
    & .szh-menu__header {
      color: rgba(7, 19, 18, 0.6);
      text-transform: none;
      /* padding: 0; */
      /* margin-bottom: 0.5rem; */
    }
    & .szh-menu__item {
      text-align: left !important;
      /* padding: 0; */
    }
  }
`;
